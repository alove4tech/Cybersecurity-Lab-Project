# Roadmap

## Near-term (current focus)
- Deploy Windows Server 2022 Active Directory lab (DC + workstation)
- Integrate Wazuh SIEM for Windows + Linux telemetry
- Forward pfSense logs to ELK (Logstash → Elasticsearch → Kibana)

## Next
- Publish detection use-cases with validation steps (attack → telemetry → alert)
- Build incident response playbooks + short case studies
- Add automation (Ansible/PowerShell/Bash) for repeatable builds
